# 競品比較圖 — 交付待辦清單

> 這份是「pitch 半成品 → 正式交付成品」之間還要做的事。
> 每完成一項就把 `[ ]` 改成 `[x]`；有新項目就往對應分類加。
> 狀態說明：✅ 已完成（pitch 階段）｜🟡 進行中｜⬜ 待辦（交付前）

最後更新：2026-06-29

---

## 0. 目前已完成（pitch 半成品）
- [x] MoneyDJ 維度覆蓋度探測腳本（`scripts/moneydj_probe.py`）
- [x] 種子 registry（`data/funds.json`，4 霸菱 + PIMCO + 高盛，真 code）
- [x] 競品比較 UI（`materials/compare.js`）：主打/競品下拉、1v1/1v1v1、同類別篩選、維度勾選、價值主張、徽章開關
- [x] 接入橫式 1920×1080 比較圖版型，管線改尺寸感知，輸出高解析圖（給簡報用）

---

## A. 輸出 / 交付格式
- [ ] **可編輯簡報檔（PPTX）** — 目前輸出圖片；客戶情境是放進簡報。用 PptxGenJS 在前端產原生 .pptx（原生表格＋文字框＋色塊，可在 PowerPoint 直接改）。同一份資料餵「圖片」與「PPTX」兩個輸出。
- [ ] （可選）PNG 高解析輸出選項（文字比 JPG 更銳利）。

## B. AI / LLM 串接 — 🚫 demo 不做，第二階段升級項目（需求驅動）
> 決策（2026-08）：demo 階段先不做 AI。改走 0727 note item 4「人工可編輯欄位」（見 §C 人工 curate）。
> 理由：(1) 客戶 0727 note 自己要求「拿掉 AI、PIC 自行輸入」；(2) demo 塞 AI 有合規風險（可能生成保證/最佳等字眼或錯誤特色，在客戶/法遵前翻車）；(3) 相依未到位——需真數據 grounding + GAS 代理 + API key；(4) 人工可編輯欄位本身就是誠實好用的 demo 版本。
> 升級觸發條件（三者到齊才做）：① MoneyDJ 真數據在跑可 grounding；② 法遵表態 AI 能碰/不能碰什麼；③ 客戶主動開口要。
> demo 銷售話術：不做但可「講」——收尾提一句「第二階段可接 AI 生成建議草稿、人工核可」，展現 roadmap 又不扛風險。
> 選型結論：若要做，用 Claude Sonnet 5（近 Opus 品質、便宜、繁中+合規可控性佳、Claude Code 開發最順）；成本在此用量是雜訊（每月個位數美金），別用價格選型。Gemini Flash 帳面最便宜且 GAS 整合最省事，是備選。
- [ ] **串接 Claude API（`claude-sonnet-5`，或 Opus 4.8 求最高品質）** 取代目前 hardcode 的價值主張下拉。
  - 放在 **GAS（UrlFetchApp）當代理**，API key 存 Script Properties，不進前端／git。
  - 用 **Structured Outputs（JSON schema）**，只擬軟性維度：利率敏感度、投資特色 bullets、價值主張。
  - 合規護欄：禁止 AI 生成／修改任何數字、禁用保證/最佳等字眼、警語由人工填；輸出標記「AI 草稿待人工核可」。
  - 把抓到的真數據當 grounding 餵進去，避免幻覺。
  - 介面：AI 生成建議 → 填進 item 4 的可編輯欄位 → PIC 編輯後拍板。地基（可編輯欄位）在 demo 就先做好，AI 這層之後直接插上。

## C. 真實資料串接（取代 mock）
- [ ] **MoneyDJ 真值接入**（經 GAS、Big5 解碼）：報酬率、波動度、Sharpe/Beta、配息率、基本資料（投資標的/區域/規模/RR/幣別）、持股配置 → 填進 `compare.js` 取代 `MOCK`。
- [ ] **人工 curate 維度的輸入介面**：MoneyDJ 抓不到的——平均信評、存續期間、利率敏感度、投資特色——在左側面板加可編輯欄位。
- [ ] **最大回撤自算**：MoneyDJ 沒有，需從淨值時間序列（`yp010001`）計算。
- [ ] **數字可人工編輯且「留痕跡」**（文件 §6）：可改但標記「已校正／記原始值」，避免無痕竄改績效。

## D. registry / 基金庫
- [ ] **`is_high_yield` 逐檔由 Rose 確認**（影響高收益債專屬警語；目前自動推測）。
- [ ] 補完整霸菱 lineup + 更多競品 code。
- [ ] 階段二：自動列舉基金宇宙（爬 MoneyDJ 境外基金排名頁，逐步擴充 registry）。

## E. 合規 / 警語（多由 Barings 法遵拍板）
- [ ] 警語清單與擺放位置由法遵決定；高收益債專屬警語依 `is_high_yield` 套用。
- [ ] 自訂報酬區間是否開放、開放到什麼程度（預設 YTD 中性區間）。
- [ ] 「同類型平均」要不要呈現在圖上（合規較安全，但弱化「霸菱領先」訴求）。
- [ ] 「綜合表現領先」徽章的最終顯示規則由法遵點頭（目前預設關）。

## F. 素材 / 視覺
- [ ] 海報 BARINGS logo 占位待換真實 logo（`assets/barings-logo.png` 也尚未放入）。
- [ ] 頁尾「資料來源／投資警語」占位 → 換成正式文字。

## G. 流程 / 系統
- [ ] 審核狀態流程（pitch 略過；完整版做「人工可審核」狀態）。
- [ ] `index.html` Creative Studio 改版：手機版 layout 待補。
- [ ] 探測腳本補進競品 code 後重跑，更新覆蓋度報表。

---

## 已知技術備註
- 比較圖「領先勾選（✔）」目前依規則自動算（波動/存續/利率敏感取低、收益/報酬/回撤取高、信評不標）；接真值後會自動重算，可能出現競品在某維度領先——屬誠實呈現，業務可關掉該維度。
- 探測腳本的子頁網址與 code 對照已校正（見 `scripts/moneydj_probe.py` 註解與 memory）。
